# ไลบรารีที่ระบบใช้ (ติดตั้งด้วย: pip install -r requirements.txt)
fastapi==0.115.6          # เว็บเฟรมเวิร์ก
uvicorn[standard]==0.34.0 # เซิร์ฟเวอร์รันเว็บในเครื่อง
jinja2==3.1.5             # เทมเพลตหน้าเว็บ HTML
python-multipart==0.0.20  # รับข้อมูลจากฟอร์ม
SQLAlchemy==2.0.36        # จัดการฐานข้อมูล SQLite
python-docx==1.1.2        # สร้าง/อ่านไฟล์เอกสาร Word (.docx)
docxtpl==0.20.2           # เติมข้อมูลลงเทมเพลต Word ด้วย {{ placeholder }}
docxcompose==1.4.0        # รวมหลายไฟล์ .docx เป็นไฟล์เดียว (ออกทั้งชุด)
openpyxl==3.1.5           # สร้างไฟล์ Excel (.xlsx) สำหรับทะเบียน
holidays==0.98            # ปฏิทินวันหยุดราชการไทย (รวมวันพระจันทร์คติ + วันชดเชย)
pdfplumber==0.11.9        # อ่านข้อความจาก PDF (นำเข้าหนังสือ AMSS อัตโนมัติ)
itsdangerous==2.2.0       # เซ็นคุกกี้ session (ระบบล็อกอินคลาวด์)
boto3==1.35.99            # อัปสำรองข้อมูลขึ้นคลาวด์ S3/R2/B2 (off-site backup)
setuptools<81             # ให้ pkg_resources ใช้ได้ (docxcompose ต้องใช้)
pytesseract==0.3.13       # OCR อ่านตัวอักษรจากรูป (ต้องมี tesseract-ocr + tesseract-ocr-tha)
Pillow==11.1.0            # เปิด/ปรับรูปภาพก่อนส่งเข้า OCR
qrcode
